[build]
builder = "nixpacks"

[deploy]
startCommand = "bun run build && bun run start:prod"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.variables]
NODE_ENV = "production"